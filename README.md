# deduper
web based deduplication application; gets an inventory of all nested files within a given root path, stores properties such as hashes of file content, file size, filename, and path. Computes potential duplicates based on hashed content and file size. 
